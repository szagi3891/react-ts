import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { App } from './App';

const root = document.getElementById('root');

ReactDOM.render(<App name="name jakies" />, root);

/*
const gg = new Promise((resolve: ((a:number)=>void)) => {
    resolve(33);
});
*/